{
  "name": "@bini-game-town/game",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "cd bini-puzzle && yarn build && cd .. && cd flappy-plane && yarn build && cd .. && mkdir -p ../dist/game/bini-puzzle && mkdir -p ../dist/game/flappy-plane && cp -r ./bini-puzzle/dist/* ../dist/game/bini-puzzle/ && cp -r ./flappy-plane/dist/* ../dist/game/flappy-plane/"
  },
  "devDependencies": {
    "typescript": "~5.7.2",
    "vite": "^6.2.0"
  }
}
