{
  "name": "@bini-game-town/game",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "cd bini-puzzle && pnpm build && cd ../flappy-plane && pnpm build && cd ../typo-trap && pnpm build"
  },
  "devDependencies": {
    "typescript": "~5.7.2",
    "vite": "^6.2.0"
  }
}
